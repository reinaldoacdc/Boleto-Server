const express = require("express");
const router = express.Router();
const controller = require("../controllers/firebaseAuthController");

// router.get("/api/users", controller.list_all_users);
// router.get("/api/user/:id", firestore.get_user);

// router.put("/api/user/update/:id", firestore.update_user);
// router.delete("/api/user/delete/:id", controller.delete_user);
// router.post("/api/user/create", controller.create_user);

module.exports = router;
